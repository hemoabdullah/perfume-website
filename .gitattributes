# Set the default behavior to have all files normalized to LF in the repository
* text=auto

# Explicitly declare text files you want to always be normalized and 
# converted to native line endings on checkout
*.css text
*.js text
*.html text
*.json text
*.md text